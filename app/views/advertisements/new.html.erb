<h1>New Advertisement</h1>

<div class="row">
  <div class="col-md-4">
    <p>All advertisements will be proofread by editor one week before publishing after which you will have the opportunity to submit edits.</p>
  </div>
</div>
<div class="col-md-8">
  <%= form_for @advertisement do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title %>
      </div>

      <div class="form-group">
        <%= f.label :copy %>
        <%= f.text_field :copy %>
      </div>

      <div class="form-group">
        <%= f.submit "Save" %>
      </div>
  <% end %>
</div>
